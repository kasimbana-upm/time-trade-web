language: node_js
node_js:
  - stable
branches:
  - develop
  - master
cache:
  directories:
    - node_modules
script:
  - npm test
  - npm run build
deploy:
  provider: heroku
  api_key: $HEROKU
  app: time-trade-web
  on:
    branch: master
